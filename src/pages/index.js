import { Button } from '@material-ui/core'

import Layout from 'src/components/Layout'

export default function Home() {
  return (
    <Layout>
      <Button variant="contained" color="primary">OK</Button>
      <h1>Projeto Independence + Next.js + Material UI</h1>
    </Layout>
  )
}